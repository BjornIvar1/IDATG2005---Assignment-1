package handler

// URL Paths as constants (for easier debugging and switching between development and production environment)
const DEFAULT_PATH = "/"
const EXCHANGE_PATH = "/exchange"
const INFO_PATH = "/info"
const STATUS_PATH = "/status"
